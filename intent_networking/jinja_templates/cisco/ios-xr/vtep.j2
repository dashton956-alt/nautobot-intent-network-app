{# vtep.j2 — ios-xr #}
! VTEP / NVE
! Intent: {{ intent_id }}
!
interface nve1
 source {{ source_interface | default("Loopback1") }}
{% for vni in vnis | default([]) %}
 member vni {{ vni.id }}
{% if vni.ingress_replication | default(false) %}
  ingress-replication bgp
{% endif %}
{% endfor %}
!
