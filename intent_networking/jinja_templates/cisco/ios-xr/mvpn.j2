{# mvpn.j2 — ios-xr #}
! MVPN Configuration
! Intent: {{ intent_id }}
!
multicast-routing
 vrf {{ vrf }}
  address-family ipv4
   interface all enable
   mdt default {{ mdt_default_group | default("") }}
{% if mdt_data_group | default("") %}
   mdt data {{ mdt_data_group }}
{% endif %}
!
