{# static_route.j2 — ios-xr #}
! Static Routes
! Intent: {{ intent_id }}
!
router static
 address-family ipv4 unicast
{% for route in routes | default([]) %}
  {{ route.prefix }} {{ route.next_hop }}{% if route.admin_distance | default("") %} {{ route.admin_distance }}{% endif %}{% if route.description | default("") %} description {{ route.description }}{% endif %}

{% endfor %}
!
