{# eigrp.j2 — ios-xr #}
! EIGRP (IOS-XR — limited support)
! Intent: {{ intent_id }}
! Note: IOS-XR has limited EIGRP support. Consider OSPF/IS-IS.
!
router eigrp {{ asn }}
 address-family ipv4
{% for iface in interfaces | default([]) %}
  interface {{ iface.name }}
{% endfor %}
!
