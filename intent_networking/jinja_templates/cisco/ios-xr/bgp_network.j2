{# bgp_network.j2 — ios-xr #}
! BGP Network Advertisement
! Intent: {{ intent_id }}
!
router bgp {{ local_asn }}
{% if vrf | default("") %}
 vrf {{ vrf }}
  address-family ipv4 unicast
{% else %}
 address-family ipv4 unicast
{% endif %}
{% for net in networks | default([]) %}
  network {{ net }}
{% endfor %}
!
