{# urpf.j2 — ios-xr #}
! uRPF
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
interface {{ iface }}
 ipv4 verify unicast source reachable-via {{ mode | default("rx") }}
{% endfor %}
!
