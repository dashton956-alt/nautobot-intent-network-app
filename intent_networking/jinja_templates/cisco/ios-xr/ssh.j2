{# ssh.j2 — ios-xr #}
! SSH Configuration
! Intent: {{ intent_id }}
!
ssh server v2
{% if timeout | default("") %}
ssh timeout {{ timeout }}
{% endif %}
{% if key_size | default("") %}
ssh server rate-limit {{ key_size | default(60) }}
{% endif %}
!
