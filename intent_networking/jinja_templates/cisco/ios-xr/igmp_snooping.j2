{# igmp_snooping.j2 — ios-xr #}
! IGMP Snooping
! Intent: {{ intent_id }}
!
igmp snooping
{% for vlan in vlans | default([]) %}
 bridge-domain BD-{{ vlan }}
{% if querier_enabled | default(false) %}
  querier address {{ querier_address | default("") }}
{% endif %}
{% endfor %}
!
