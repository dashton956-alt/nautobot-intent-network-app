{# ip_sla.j2 — ios-xr #}
! IP SLA
! Intent: {{ intent_id }}
!
ipsla
 operation {{ sla_id }}
  type icmp echo
   destination {{ target }}
   timeout {{ timeout | default(5000) }}
   frequency {{ frequency | default(60) }}
  schedule operation {{ sla_id }}
   start-time now
   life forever
!
