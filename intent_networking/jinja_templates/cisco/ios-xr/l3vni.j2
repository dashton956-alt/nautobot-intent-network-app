{# l3vni.j2 — ios-xr #}
! L3 VNI (VXLAN)
! Intent: {{ intent_id }}
!
vrf {{ vrf_name }}
 address-family ipv4 unicast
  import route-target
   {{ rt_import | default("auto") }}
  export route-target
   {{ rt_export | default("auto") }}
!
