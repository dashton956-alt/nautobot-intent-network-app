{# bgp_evpn_af_removal.j2 — nxos #}
! Remove BGP EVPN AF
! Intent: {{ intent_id }}
!
router bgp {{ local_asn }}
{% for neighbor in neighbors | default([]) %}
  no neighbor {{ neighbor.ip }}
{% endfor %}
  no address-family l2vpn evpn
!
