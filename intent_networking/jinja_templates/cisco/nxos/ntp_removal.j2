{# ntp_removal.j2 — nxos #}
! Remove NTP
! Intent: {{ intent_id }}
!
{% for server in servers | default([]) %}
no ntp server {{ server }}
{% endfor %}
{% if source_interface | default("") %}
no ntp source-interface {{ source_interface }}
{% endif %}
!
