{# multicast_vrf.j2 — ios-xe #}
! Multicast VRF
! Intent: {{ intent_id }}
!
ip multicast-routing vrf {{ vrf }}
ip pim vrf {{ vrf }} rp-address {{ rp_address }}
!
{% for iface in interfaces | default([]) %}
interface {{ iface.name }}
 vrf forwarding {{ vrf }}
 ip pim sparse-mode
{% endfor %}
!
