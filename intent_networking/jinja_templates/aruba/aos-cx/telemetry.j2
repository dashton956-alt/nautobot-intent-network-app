{# telemetry.j2 — aos-cx #}
! Network Analytics Engine (Telemetry)
! Intent: {{ intent_id }}
!
{% for agent in agents | default([]) %}
nae-agent {{ agent.name | default("NAE-AGENT-" ~ loop.index) }}
{% if agent.script | default("") %}
    script {{ agent.script }}
{% endif %}
{% endfor %}
