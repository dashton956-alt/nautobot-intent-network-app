{# zbf.j2 — aos-cx #}
! Zone-Based Firewall (not fully supported on AOS-CX)
! Intent: {{ intent_id }}
! Note: AOS-CX uses ACLs for traffic filtering rather than zone-based firewall.
{% for entry in entries | default([]) %}
{% if entry.acl | default("") %}
access-list ip {{ entry.acl }}
{% endif %}
{% endfor %}
