{# vtep.j2 — aos-cx #}
! VTEP / VXLAN Tunnel
! Intent: {{ intent_id }}
!
interface vxlan 1
    source ip {{ source_ip | default("") }}
    no shutdown
{% for vni in vnis | default([]) %}
    vni {{ vni.id }}
{% if vni.vlan_id | default("") %}
        vlan {{ vni.vlan_id }}
{% endif %}
{% endfor %}
