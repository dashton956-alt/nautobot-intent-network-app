{# qinq.j2 — aos-cx #}
! QinQ (Double VLAN Tagging)
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
interface {{ iface.name }}
    vlan trunks {{ iface.outer_vlan | default("") }} native
    vlan dot1q-tunnel {{ iface.inner_vlan | default("") }}
{% endfor %}
