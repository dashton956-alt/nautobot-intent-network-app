{# prefix_list.j2 — aos-cx #}
! Prefix List
! Intent: {{ intent_id }}
!
{% for entry in entries | default([]) %}
ip prefix-list {{ prefix_list_name }} seq {{ entry.sequence | default(loop.index * 10) }} {{ entry.action | default("permit") }} {{ entry.prefix }}{% if entry.ge | default("") %} ge {{ entry.ge }}{% endif %}{% if entry.le | default("") %} le {{ entry.le }}{% endif %}

{% endfor %}
