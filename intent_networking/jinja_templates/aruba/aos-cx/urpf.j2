{# urpf.j2 — aos-cx #}
! uRPF Configuration
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
interface {{ iface }}
    ip verify unicast source reachable-via {{ mode | default("rx") }}
{% endfor %}
