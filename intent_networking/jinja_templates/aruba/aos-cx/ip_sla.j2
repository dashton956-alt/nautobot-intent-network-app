{# ip_sla.j2 — aos-cx #}
! IP SLA (Loop-Protect/Link Keepalive)
! Intent: {{ intent_id }}
!
{% for probe in probes | default([]) %}
ip route {{ probe.target }}/32 {{ probe.next_hop | default("") }}
! Note: AOS-CX uses loop-protect and link-keepalive for link monitoring
{% endfor %}
