{# nat.j2 — aos-cx #}
! NAT (not fully supported on AOS-CX campus switches)
! Intent: {{ intent_id }}
! Note: NAT is limited on AOS-CX. Use a firewall or router for NAT.
{% for rule in rules | default([]) %}
! Rule: {{ rule.name | default("NAT-RULE") }} - {{ rule.type | default("source") }}
{% endfor %}
