{# isis_removal.j2 — arista-eos #}
! Remove IS-IS Configuration
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
interface {{ iface.name }}
   no isis enable
   no isis metric
   no isis network
   no isis passive
!
{% endfor %}
no router isis {{ process_tag }}
!
