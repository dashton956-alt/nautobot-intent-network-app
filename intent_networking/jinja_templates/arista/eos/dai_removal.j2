{# dai_removal.j2 — arista-eos #}
! Remove DAI Configuration
! Intent: {{ intent_id }}
!
{% for iface in trusted_interfaces | default([]) %}
interface {{ iface }}
   no ip arp inspection trust
!
{% endfor %}
{% for vlan in vlans | default([]) %}
no ip arp inspection vlan {{ vlan }}
{% endfor %}
!
