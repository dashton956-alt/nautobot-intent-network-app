{# bgp_neighbor_removal.j2 — arista-eos #}
! Remove BGP Neighbor — {{ neighbor_ip }}
! Intent: {{ intent_id }}
!
router bgp {{ local_asn }}
{% if vrf_name %}
   vrf {{ vrf_name }}
{% endif %}
   no neighbor {{ neighbor_ip }}
!
