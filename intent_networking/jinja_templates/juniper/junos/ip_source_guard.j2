{# ip_source_guard.j2 — junos #}
! IP Source Guard
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
set ethernet-switching-options secure-access-port interface {{ iface }} ip-source-guard
{% endfor %}
