{# multicast_vrf.j2 — junos #}
! Multicast VRF
! Intent: {{ intent_id }}
!
{% if vrf | default("") %}
set routing-instances {{ vrf }} protocols pim
{% if rp_address | default("") %}
set routing-instances {{ vrf }} protocols pim rp static address {{ rp_address }}
{% endif %}
{% endif %}
