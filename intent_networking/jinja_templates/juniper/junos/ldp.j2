{# ldp.j2 — junos #}
! LDP Configuration
! Intent: {{ intent_id }}
!
{% for iface in interfaces | default([]) %}
set protocols ldp interface {{ iface }}
{% endfor %}
{% if transport_address | default("") %}
set protocols ldp transport-address {{ transport_address }}
{% endif %}
