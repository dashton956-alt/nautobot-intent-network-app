{# mvpn.j2 — junos #}
! MVPN Configuration
! Intent: {{ intent_id }}
!
set routing-instances {{ vrf }} protocols pim
set routing-instances {{ vrf }} provider-tunnel rsvp-te
{% if mdt_default_group | default("") %}
set routing-instances {{ vrf }} provider-tunnel rsvp-te label-switched-path-template default-template
{% endif %}
