{# ssl_inspection.j2 — junos #}
! SSL Inspection (SSL Proxy)
! Intent: {{ intent_id }}
!
{% if ssl_proxy | default(false) %}
set services ssl proxy profile {{ profile_name | default("SSL-PROXY") }}
{% endif %}
