# -----------------------------------------------------------------------------
# Intent type: sdwan_dia
# Purpose:     Direct Internet Access breakout for trusted SaaS.
# -----------------------------------------------------------------------------

id: acme-sdwan-dia-001
type: sdwan_dia
version: 1
tenant: acme-corp
change_ticket: CHG0031020
description: "SD-WAN DIA — local internet breakout with DNS redirect"

scope:
  devices: [branch-lon-sdwan-01]

nat_interface: GigabitEthernet0/0/1
dns_redirect: true
breakout_applications:
  - office365
  - webex
  - salesforce

policy:
  compliance: NONE
  encryption: none

deployment:
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
