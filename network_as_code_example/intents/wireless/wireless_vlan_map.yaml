# -----------------------------------------------------------------------------
# Intent type: wireless_vlan_map
# Purpose:     Map the corporate SSID to a dynamically allocated wireless VLAN.
# -----------------------------------------------------------------------------

id: acme-wl-vlan-map-001
type: wireless_vlan_map
version: 1
tenant: acme-corp
change_ticket: CHG0031010
description: "Wireless VLAN map — CORP-WIFI to allocated VLAN"

scope:
  devices: [wlc-dc-east-01]

ssid_name: CORP-WIFI

policy:
  compliance: NONE
  encryption: none

deployment:
  controller: wlc
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
