# -----------------------------------------------------------------------------
# Intent type: multicast_pim_ssm
# Purpose:     PIM source-specific multicast for IPTV.
# -----------------------------------------------------------------------------

id: acme-mc-pim-ssm-001
type: multicast_pim_ssm
version: 1
tenant: acme-corp
change_ticket: CHG0031039
description: "PIM-SSM — IPTV delivery in the default SSM range"

scope:
  devices: [dc-east-core-01]

ssm_range: "232.0.0.0/8"
interfaces:
  - Ethernet1
  - Ethernet2

policy:
  compliance: NONE
  encryption: none

deployment:
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
