# -----------------------------------------------------------------------------
# Intent type: ldp
# Purpose:     LDP label distribution on core-facing links.
# -----------------------------------------------------------------------------

id: acme-mpls-ldp-001
type: ldp
version: 1
tenant: acme-corp
change_ticket: CHG0031044
description: "LDP — enable label distribution on the P/PE core links"

scope:
  devices: [pe-router-lon-01]

router_id: 10.255.0.1
interfaces:
  - Ethernet1
  - Ethernet2
targeted_sessions:
  - 10.255.0.2
label_allocation: default

policy:
  compliance: NONE
  encryption: none

deployment:
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
