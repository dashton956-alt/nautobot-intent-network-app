# -----------------------------------------------------------------------------
# Intent type: rsvp_te
# Purpose:     RSVP-TE tunnel with bandwidth reservation.
# -----------------------------------------------------------------------------

id: acme-mpls-rsvp-te-001
type: rsvp_te
version: 1
tenant: acme-corp
change_ticket: CHG0031047
description: "RSVP-TE — 500 Mbps TE tunnel to the remote PE"

scope:
  devices: [pe-router-lon-01]

tunnel_destination: 10.255.0.2
bandwidth: "500000"
path_option: dynamic
setup_priority: 5
hold_priority: 5
affinity: "0x0"

policy:
  compliance: NONE
  encryption: none

deployment:
  controller: nornir
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
