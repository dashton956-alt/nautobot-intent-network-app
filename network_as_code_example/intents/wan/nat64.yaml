# -----------------------------------------------------------------------------
# Intent type: nat64
# Purpose:     Stateful NAT64 for IPv6-only clients.
# -----------------------------------------------------------------------------

id: acme-wan-nat64-001
type: nat64
version: 1
tenant: acme-corp
change_ticket: CHG0031049
description: "NAT64 — stateful translation for the v6-only user segment"

scope:
  devices: [dc-east-edge-01]

prefix: "64:ff9b::/96"
mode: stateful
v4_pool: "203.0.113.64/27"
interfaces:
  - Ethernet1
  - Ethernet2

policy:
  compliance: NONE
  encryption: none

deployment:
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
