# -----------------------------------------------------------------------------
# Intent type: reachability
# Purpose:     Legacy bare 'reachability' type — dispatches on reachability_type.
# -----------------------------------------------------------------------------

id: acme-reachability-001
type: reachability
version: 1
tenant: acme-corp
change_ticket: CHG0031033
description: "Reachability (legacy dispatch) — static default toward the DC"

scope:
  devices: [branch-lon-rtr-01]

reachability_type: static
routes:
  - prefix: "10.0.0.0/8"
    next_hop: 10.60.0.1

policy:
  compliance: NONE
  encryption: none

deployment:
  controller: nornir
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
