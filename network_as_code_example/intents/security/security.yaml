# -----------------------------------------------------------------------------
# Intent type: security
# Purpose:     Legacy bare 'security' type — site-scoped segmentation ACLs.
# -----------------------------------------------------------------------------

id: acme-sec-legacy-001
type: security
version: 1
tenant: acme-corp
change_ticket: CHG0031057
description: "Security (legacy) — segmentation ACL for the DC East site"

scope:
  sites: [dc-east]

isolation:
  deny_protocols: [telnet, ftp]
destination:
  prefixes:
    - "10.20.0.0/16"

policy:
  compliance: NONE
  encryption: none

deployment:
  strategy: rolling
  save_config: true

verification:
  level: basic
  trigger: on_deploy
  fail_action: alert
