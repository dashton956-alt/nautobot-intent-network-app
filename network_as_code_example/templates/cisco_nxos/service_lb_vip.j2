{# service_lb_vip — Load balancer VIP configuration #}
{# Cisco NX-OS does not manage load balancer VIPs directly. #}
{# VIP configuration is deployed via the load balancer controller API (e.g., F5, AVI). #}
{# No CLI output is generated for NX-OS. #}
