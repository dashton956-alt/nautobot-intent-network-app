{# service_lb_vip — Load balancer VIP configuration #}
{# Cisco IOS-XE does not natively manage load balancer VIPs. #}
{# VIP configuration is handled by the load balancer controller (e.g., F5 BIG-IP, Citrix ADC). #}
{# This intent type is deployed via the LB controller API, not via device CLI. #}
{# No CLI output is generated for IOS-XE. #}
